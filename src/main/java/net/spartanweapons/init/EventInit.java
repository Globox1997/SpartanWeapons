package net.spartanweapons.init;

@SuppressWarnings("unused")
public class EventInit {

    public static void init() {
//        LootTableEvents.MODIFY.register((key, tableBuilder, source, registries) -> {
//         if (key.getValue().getPath().contains("_leaves")&&ItemInit.WOOD_MATERIALS.contains(key.getValue().getPath())&& ConfigInit.CONFIG.stickDropChance > 0.0001f) {
//             LootPool pool = LootPool.builder().with(ItemEntry.builder(Registries.ITEM.get(SpartanWeaponsMain.identifierOf())).build()).rolls(BinomialLootNumberProvider.create(1, ConfigInit.CONFIG.stickDropChance)).build();
//             tableBuilder.pool(pool);
//         }
//         });
    }
}
